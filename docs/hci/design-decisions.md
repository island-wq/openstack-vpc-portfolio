---
title: 핵심 설계 결정
description: HCI 상품 설계의 주요 결정과 트레이드오프
---

# 핵심 설계 결정

## ADR-01. 3노드 HCI 클러스터로 시작

**결정:** 관리·배포 노드는 별도로 두고, 3개 노드에 Controller, Compute, Ceph 역할을 통합합니다.

**이유:** OpenStack Control Plane과 Ceph의 Quorum을 유지하면서 초기 장비 수를 최소화할 수 있습니다.

**트레이드오프:** 노드 1대 장애 시 컴퓨트와 스토리지 여유가 함께 감소하므로 N+1 용량 산정이 필수입니다.

## ADR-02. 1U 듀얼 소켓 서버를 기준 플랫폼으로 사용

**결정:** CPU, 메모리, 디스크, NIC를 확장할 수 있는 1U 듀얼 소켓 서버를 기준으로 합니다.

**이유:** 제한된 랙 공간에서 컴퓨트 집적도와 HCI용 로컬 디스크 수를 함께 확보하기 위해서입니다.

**트레이드오프:** 특정 모델에 종속되지 않도록 CPU 세대, 디스크 슬롯, NIC 포트 수를 최소 인증 기준으로 관리해야 합니다.

## ADR-03. Small 상품의 기본 대역폭을 10GbE로 제한

**결정:** 초기 상품은 10GbE와 LACP를 기본으로 하고, 더 높은 대역폭은 상위 상품으로 분리합니다.

**이유:** 소규모 고객의 비용과 상품 라인업 구분을 고려한 선택입니다.

**트레이드오프:** VM, Geneve, Ceph, 복구 트래픽이 동일 링크를 공유하므로 부하 테스트와 확장 기준이 필요합니다.

## ADR-04. ML2/OVN과 Geneve Overlay 사용

**결정:** Tenant 네트워크는 Geneve Overlay로 제공하고, OVN DVR을 사용합니다.

**이유:** VLAN 수 제약을 줄이고 East-West 및 Floating IP를 Compute Node에서 분산 처리할 수 있습니다.

**트레이드오프:** MTU, OVN Database, Gateway Chassis 운영 복잡성이 증가합니다.

## ADR-05. PXE를 기본 배포 방식으로 표준화

**결정:** 반복 설치는 PXE로 처리하고, IPMI Virtual Media는 예외 절차로 사용합니다.

**이유:** 장비 수가 늘어나도 동일한 이미지와 패키지로 일관된 설치가 가능하기 때문입니다.

**트레이드오프:** DHCP, TFTP, Repository의 가용성과 네트워크 접근 통제가 추가로 필요합니다.

## ADR-06. 상품 규모가 커지면 역할 분리형으로 전환

**결정:** HCI 노드 증설만으로 모든 규모를 수용하지 않고, 임계점을 넘으면 Controller·Compute·Storage 분리형 상품으로 전환합니다.

**이유:** 대규모 환경에서는 장애 영향도와 자원 경합을 줄이는 것이 장비 수 절감보다 중요해집니다.

